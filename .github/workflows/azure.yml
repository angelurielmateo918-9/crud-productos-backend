name: Deploy to Azure
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: azure/webapps-deploy@v2
      with:
        app-name: 'crud-productos-backend'
        publish-profile: ${{ secrets.AZURE_PUBLISH_PROFILE }}
